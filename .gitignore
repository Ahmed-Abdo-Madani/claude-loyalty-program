# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Production builds
/dist
/build

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Google Cloud credentials (NEVER COMMIT THESE!)
madna-platform-*.json
google-credentials.json
google wallet creds.md
credentials/

# Allow package.json and package-lock.json (lock files must be committed per npm best practices)
# package-lock.json - REMOVED: Lock files are required for reproducible builds

# Wallet pass files (generated/test files)
*.pkpass
test-*.pkpass

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log

# Temporary files
*.tmp
*.temp
nul
cookies.txt
_tmp_*
backend/_tmp_*

# Claude specific
.claude/

# Test output files
tests/*.json
tests/memory-snapshots.json
tests/location-service-*.json

# Development/Test files
test-*.js
**/test-*.js
decode-jwt-test.js
setup-customer-db.js
QR-SCANNER-ISSUES-ANALYSIS.md

# Development documentation (keep only main README)
wireframes-*.md
SCALABILITY-ROADMAP.md

# Production deployment files (keep local)
# render.yaml - COMMENTED OUT: Now using Blueprint deployment (render.yaml must be committed)
.env.production
deployment/

# Windows batch files
*.bat
*.ps1

# Database dumps and backups
*.sql
*.dump
backup/

# Apple Wallet Certificates - NEVER COMMIT THESE!
backend/certificates/*.p12
backend/certificates/*.pem
backend/certificates/*.key
backend/certificates/*.cer
backend/certificates/*.csr

# APNs Certificates (NEVER commit these!)
backend/certificates/apns/*.key
backend/certificates/apns/*.p12
backend/certificates/apns/*.pem
backend/certificates/apns/*.cer
backend/certificates/apns/*.csr

# Certificate environment variables - SENSITIVE DATA!
backend/scripts/certificate-env-vars.txt