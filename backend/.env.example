# Server Configuration
PORT=3001
NODE_ENV=development
BASE_URL=http://localhost:3001

# Apple Wallet Configuration - REQUIRED FOR ALL ENVIRONMENTS
APPLE_PASS_TYPE_ID=pass.com.loyaltyplatform.storecard
APPLE_TEAM_ID=YOUR_TEAM_ID
APPLE_PASS_CERTIFICATE_PASSWORD=your_certificate_password

# Apple Wallet Configuration - DEVELOPMENT (file-based)
# These paths are used when NODE_ENV != production
APPLE_PASS_CERTIFICATE_PATH=./certificates/pass.p12
APPLE_WWDR_CERTIFICATE_PATH=./certificates/AppleWWDRCAG4.pem

# Apple Wallet Configuration - PRODUCTION (base64-encoded)
# Use backend/scripts/encode-certificates.js to generate these values
# APPLE_CERT_P12_BASE64=MIIKmwIBAzCC...
# APPLE_CERT_WWDR_BASE64=LS0tLS1CRUd...

# Google Wallet Configuration (Production)
# GOOGLE_SERVICE_ACCOUNT_EMAIL=wallet-service@your-project.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY_PATH=./certificates/google-service-account-key.json
# GOOGLE_PROJECT_ID=your-google-cloud-project-id

# Wallet Notification Settings
WALLET_NOTIFICATION_DAILY_LIMIT=10

# Auto-Engagement Configuration
# Set to 'true' to disable daily auto-engagement cron job
DISABLE_AUTO_ENGAGEMENT=false

# File System Paths
# Icons Path Configuration
# Override the default icons directory path if needed
# Production (Docker): /app/uploads/icons/stamps
# Development: Automatically resolved relative to project structure
# Set this in production if the default path resolution fails
# Docker deployments should use: /app/uploads/icons/stamps
# Leave unset for development environments
# ICONS_PATH=/app/uploads/icons/stamps

# Database Configuration (Future)
# DATABASE_URL=postgresql://username:password@localhost:5432/loyalty_platform

# Security - JWT Secret for Token Signing
# CRITICAL: Change this to a strong random secret in production (min 32 characters)
# Development: The default below works for local testing
# Production: Use a cryptographically secure random string
# Generate with: openssl rand -base64 32
JWT_SECRET=dev-jwt-secret-change-in-production-min-32-chars
# ENCRYPTION_KEY=your-encryption-key-for-sensitive-data

# External Services (Future)
# SENDGRID_API_KEY=your-sendgrid-api-key
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Development Settings
DEMO_MODE=true
LOG_LEVEL=debug